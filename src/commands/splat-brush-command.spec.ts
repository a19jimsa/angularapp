import { SplatBrushCommand } from './splat-brush-command';

describe('SplatBrushCommand', () => {
  it('should create an instance', () => {
    expect(new SplatBrushCommand()).toBeTruthy();
  });
});
