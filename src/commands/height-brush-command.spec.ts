import { HeightBrushCommand } from './height-brush-command';

describe('HeightBrushCommand', () => {
  it('should create an instance', () => {
    expect(new HeightBrushCommand()).toBeTruthy();
  });
});
