import { AttackDurationSystem } from './attack-duration-system';

describe('AttackDurationSystem', () => {
  it('should create an instance', () => {
    expect(new AttackDurationSystem()).toBeTruthy();
  });
});
