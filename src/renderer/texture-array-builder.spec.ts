import { TextureArrayBuilder } from './texture-array-builder';

describe('TextureArrayBuilder', () => {
  it('should create an instance', () => {
    expect(new TextureArrayBuilder()).toBeTruthy();
  });
});
