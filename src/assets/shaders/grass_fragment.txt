#version 300 es
precision mediump float;

out vec4 FragColor;

void main() {
    //float scale = 0.8;
    //float blend = smoothstep(0.2, 0.8, v_random_Id);
    //vec3 color = mix(vec3(0.0, 0.5, 0.0), vec3(0.3, 0.9, 0.1), blend);
    //vec3 finalColor = mix(vec3(0.0, 0.5, 0.0), color, v_height / 2.0); 
    //gl_FragColor = texColor;
    FragColor = vec4(1.0);
}