#version 300 es
precision mediump float;
precision mediump sampler2DArray;

uniform sampler2DArray u_textures;
uniform float u_time;

in vec2 v_texcoord;

out vec4 FragColor;

void main() { 
    vec2 uv = v_texcoord;
    vec2 uvScrolling = v_texcoord + vec2(0.0, -u_time * 0.001); 
    vec4 baseColor = texture(u_textures, vec3(uvScrolling, 0));
    vec4 multColor = texture(u_textures, vec3(uv, 1));
    float opacity = baseColor.r + 0.5;
    opacity *= multColor.r;
    FragColor = vec4(0.0,0.2,1.0, opacity);
}