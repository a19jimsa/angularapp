#version 300 es
layout(location = 0) in vec3 a_position;
layout(location = 1) in vec2 a_texcoord;
layout(location = 2) in vec3 a_instances; // Instancevariable

uniform mat4 u_matrix;
uniform mat4 u_model;

out vec2 v_texcoord;
out float v_lifetime;
out float v_age;
 
void main() {
    v_texcoord = a_texcoord;
    gl_Position = u_matrix * u_model * vec4(a_position + a_instances, 1.0);
}