import { AnimationPlayerManager } from './animation-player-manager';

describe('AnimationPlayerManager', () => {
  it('should create an instance', () => {
    expect(new AnimationPlayerManager()).toBeTruthy();
  });
});
