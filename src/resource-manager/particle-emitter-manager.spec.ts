import { ParticleEmitterManager } from './particle-emitter-manager';

describe('ParticleEmitterManager', () => {
  it('should create an instance', () => {
    expect(new ParticleEmitterManager()).toBeTruthy();
  });
});
