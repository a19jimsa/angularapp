import { InitializationSystem } from './initialization-system';

describe('InitializationSystem', () => {
  it('should create an instance', () => {
    expect(new InitializationSystem()).toBeTruthy();
  });
});
