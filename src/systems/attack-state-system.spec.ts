import { AttackStateSystem } from './attack-state-system';

describe('AttackStateSystem', () => {
  it('should create an instance', () => {
    expect(new AttackStateSystem()).toBeTruthy();
  });
});
