import { TrailRendererSystem } from './trail-renderer-system';

describe('TrailRendererSystem', () => {
  it('should create an instance', () => {
    expect(new TrailRendererSystem()).toBeTruthy();
  });
});
