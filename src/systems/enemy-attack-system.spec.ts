import { EnemyAttackSystem } from './enemy-attack-system';

describe('EnemyAttackSystem', () => {
  it('should create an instance', () => {
    expect(new EnemyAttackSystem()).toBeTruthy();
  });
});
